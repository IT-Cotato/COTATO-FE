version: 0.0
os: linux
files:
  - source: /
    destination: /frontend/production/build
    overwrite: yes
file_exists_behavior: OVERWRITE

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu
